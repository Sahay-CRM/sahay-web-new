interface StateDataProps {
  stateId?: string;
  stateName: string;
  countryId: string;
  countryName?: string;
}

interface UpdateState {
  stateId?: string;
  stateName: string;
  countryId: string;
}

interface CityDataProps {
  cityId: string;
  cityName: string;
}
